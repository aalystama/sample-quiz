import { Spinner as BSpinner } from "react-bootstrap";
import styled from "styled-components";

export const Spinner = styled(BSpinner)`
  display: block;
  margin-left: auto;
  margin-right: auto;
`;
